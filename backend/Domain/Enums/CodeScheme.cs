namespace Backend.Domain.Enums;

public enum CodeScheme
{
    UNLOCODE = 0,     // TRIZM vb.
    UIC = 1,          // UIC station code
    RNE = 2,          // RNE location code
    IATA = 3,         // Havaalanı vb. (isteğe bağlı)
    Custom = 9
}
